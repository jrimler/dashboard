node_modules/
dist/
.env
.env.local

# ASAP exports dropped in for local analysis — contain student PII, never commit
*.xls
*.xlsx

# Playwright screenshots from scripts/screenshot.mjs — local visual checks, not artifacts
screenshots/

# Hand-built student rosters used to reconcile report counts — contain student
# names, same reason the ASAP exports above are ignored. Never commit.
*student list*.txt
